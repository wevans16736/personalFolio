{
  "name": "terminal",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "gh-pages": "^6.3.0",
    "typescript": "^5.2.2",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
